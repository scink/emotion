import * as React from 'react';
import {PureComponent} from 'react';
import {Button} from '../button/button.component';

export class FirstExample extends PureComponent {
	public render() {
		return (
			<Button>start</Button>
		);
	}
}